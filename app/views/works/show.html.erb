<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @work.title %>
</p>

<p>
  <strong>Description:</strong>
  <%= @work.description %>
</p>
<% if user_signed_in? %>
    <div id="admin_links">
        <%= link_to "Edit Work", edit_post_path(@post) %>
        <%= link_to 'Back', posts_path %>
        <%= link_to "Delete Work", posts_path(@post), method: :delete, data: {confirm: "Are you sure?" } %>
    </div>
<% end %>
<%#= link_to 'Edit', edit_work_path(@work) %> |
<%#= link_to 'Back', works_path %>
